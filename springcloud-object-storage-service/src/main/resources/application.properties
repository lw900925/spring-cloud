# Spring application
spring.application.name=springcloud-object-storage-service

# Server port
server.port=8001

management.security.enabled=false

# Spring cloud eureka discovery
eureka.instance.prefer-ip-address=true

eureka.client.service-url.defaultZone=http://localhost:8761/eureka/

# Spring RabbitMQ
spring.rabbitmq.host=10.145.4.171
spring.rabbitmq.port=5672
spring.rabbitmq.username=guest
spring.rabbitmq.password=guest
spring.rabbitmq.virtual-host=/

# Spring logging
logging.file=${user.home}/spring-cloud-logs/${spring.application.name}-${server.port}.log

# Spring zipkin
#spring.zipkin.locator.discovery.enabled=true
#spring.zipkin.service.name=springcloud-zipkin-server